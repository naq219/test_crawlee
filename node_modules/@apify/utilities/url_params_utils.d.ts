/**
 * Encodes object (e.g. input for actor) to a string hash.
 */
export declare function encodeInput<T extends object>(input: T): string;
/**
 * Decodes a string hash produced via `encodeInput` back into the original object.
 */
export declare function decodeInput(urlHash: string): any;
/**
 * Extract import statements from the code.
 */
export declare function separateImports(code: string): {
    code: string;
    imports: string;
};
